<div class="flickr">
  <ul>
    <% photos.each do |p| %>
      <% info = flickr.photos.getInfo(photo_id: p.id) %>
      <%= link_to(image_tag(FlickRaw.url_b(info), class: "flickr_photo", title: p.title, border: 0, size: "75x75"), FlickRaw.url_photopage(info)) %>
    <% end %>
  </ul>
</div>